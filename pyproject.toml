[project]
name = "eo-re-mcp"
version = "1.0.0"
description = "eo-re-mcp: multi-backend reverse-engineering MCP server (fork of re-mcp)"
readme = "README.md"
license = "MIT OR Apache-2.0"
license-files = ["LICENSE", "LICENSES/*"]
authors = [
    { name = "Joe T. Sylve, Ph.D.", email = "joe.sylve@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "eo-re-mcp-core",
    "eo-re-mcp-ida",
    "eo-re-mcp-ghidra",
]

[project.urls]
Homepage = "https://github.com/offense360/eo-re-mcp"
Repository = "https://github.com/offense360/eo-re-mcp"
Issues = "https://github.com/offense360/eo-re-mcp/issues"
Documentation = "https://github.com/offense360/eo-re-mcp/tree/main/docs"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []

[tool.uv.workspace]
members = [
    "packages/re-mcp-core",
    "packages/re-mcp-ida",
    "packages/re-mcp-ghidra",
]

[tool.uv.sources]
eo-re-mcp-core = { workspace = true }
eo-re-mcp-ida = { workspace = true }
eo-re-mcp-ghidra = { workspace = true }

[dependency-groups]
dev = [
    "eo-re-mcp-core",
    "eo-re-mcp-ida",
    "eo-re-mcp-ghidra",
    "jsonschema>=4.20,<5",
    "pre-commit>=4.5,<5",
    "pytest>=9.0,<10",
    "pytest-asyncio>=1.0,<2",
    "ruff>=0.15,<0.16",
]


[tool.pytest.ini_options]
markers = [
    "posix_only(reason): requires POSIX semantics (signals, file modes, path flavour); skipped on win32 with the given reason (issue #16)",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # Pyflakes
    "W",    # pycodestyle warnings
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
    "C4",   # flake8-comprehensions
    "RUF",  # Ruff-specific rules
    "PIE",  # flake8-pie
    "PLC",  # Pylint conventions
    "PLE",  # Pylint errors
    "PLW",  # Pylint warnings
    "FURB", # refurb
    "PERF", # perflint
    "ASYNC", # flake8-async
]
ignore = [
    "E501",    # line length handled by formatter
    "UP035",   # don't force typing-extensions imports
    "ASYNC222", # os.waitpid with WNOHANG is non-blocking
    "ASYNC240", # os.path string ops (realpath/expanduser) don't do I/O
]

[tool.ruff.lint.isort]
known-third-party = [
    "fastmcp", "mcp",
    "pyghidra", "ghidra", "java",
    "idapro", "idc", "idautils",
    "ida_auto", "ida_bytes", "ida_dirtree", "ida_diskio", "ida_entry",
    "ida_enum", "ida_fixup", "ida_fpro", "ida_frame", "ida_funcs", "ida_gdl",
    "ida_hexrays", "ida_ida", "ida_idaapi", "ida_idp", "ida_kernwin", "ida_lines",
    "ida_loader", "ida_nalt", "ida_name", "ida_problems", "ida_range",
    "ida_regfinder", "ida_search", "ida_segment", "ida_srclang", "ida_strlist",
    "ida_struct", "ida_tryblks", "ida_typeinf", "ida_ua", "ida_undo", "ida_xref",
]
